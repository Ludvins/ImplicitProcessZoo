# UCI Regression

The scalar-regression benchmark supports the shared eight-model suite on the
canonical UCI datasets.

See the [UCI regression guide](https://ludvins.github.io/ImplicitProcessZoo/experiments/uci/)
for datasets, model flags, GMVIP configuration, outputs, and commands.

```bash
python -m experiments.uci.benchmark --help
```
